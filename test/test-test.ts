import { assert } from 'chai'
import 'mocha'

// TODO(austin-king): This is a placeholder test to get CI running. Remove this file when actual tests exist.
describe('test test', function(): void {
  it('always passes', function(): void {
    assert.equal(1, 1)
  })
})
